// DaRQ Statements
reserve foreach as			"for";
reserve fault as			"throw";


// JS Statements
reserve if as				"if";
reserve else as				"else";
reserve switch as			"switch";
reserve case as				"case";
reserve default as			"default";

reserve for as				"for";
reserve of as				"of";
reserve in as				"in";
reserve while as			"while";
reserve do as				"do";
reserve break as			"break";
reserve continue as			"continue";

reserve try as				"try";
reserve catch as			"catch";
reserve finally as			"finally";
reserve throw as			"throw";

reserve return as			"return";
reserve yield as			"yield";
reserve await as			"await";
reserve async as			"async";

reserve function as			"function";
reserve class as			"class";
reserve enum as				"enum";
reserve extends as			"extends";
reserve super as			"super";
reserve new as				"new";
reserve this as				"this";

reserve import as			"import";
reserve export as			"export";

reserve const as			"const";
reserve let as				"let";
reserve var as				"var";

reserve true as				"true";
reserve false as			"false";
reserve undefined as		"undefined";
reserve null as				"null";

reserve void as				"void";
reserve delete as			"delete";
reserve typeof as			"typeof";
reserve instanceof as		"instanceof";

reserve with as				"with";
reserve debugger as			"debugger";

reserve get as				"get";
reserve set as				"set";

reserve static as			"static";

reserve implements as		"implements";
reserve interface as		"interface";
reserve package as			"package";
reserve private as			"private";
reserve protected as		"protected";
reserve public as			"public";